#include<bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin>>t;
    while (t--)
    {
        int n;
        vector<int>a, b;
        for(int i=0; i<n; i++){
            int x, y;
            cin>>x>>y;
            a.push_back(x);
            b.push_back(y);
        }
    }
	return 0;
}